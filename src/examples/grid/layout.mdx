import React from 'react';
import Grid from '@bancakit/grid';

export default Example() {
  return (
    <Grid
      gap={1}
      areas={`
        "a a a"
        "b c c"
        "d d d"
      `}
    >
      <header>Header</header>
      <aside>Aside</aside>
      <main>Main</main>
      <footer>Footer</footer>
    </Grid>
  )
}
