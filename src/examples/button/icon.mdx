import React from 'react';
import Button from '@bancakit/button';
import Icon from '@bancakit/icon';

export default function Example() {
  return (
   <>
      <Button leftIcon={<Icon name="home-outline" color="white" />}>
        Left Icon
      </Button>
      <Button rightIcon={<Icon name="home-outline" color="white" />}>
        Right Icon
      </Button>
   </>
  )
}
